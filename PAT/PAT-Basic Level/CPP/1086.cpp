#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main()
{
	int a,b, result;
	cin>>a>>b;
	result = a*b;
	string s = to_string(sum);
	reverse(s.begin(),s.end());
	cout<<stoi(s);
	return 0;
}
